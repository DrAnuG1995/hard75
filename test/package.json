{
  "name": "hard75-test",
  "private": true,
  "type": "module",
  "description": "Browser harness for ../index.html. The app itself has no dependencies and no build step; this directory exists only to run the checks.",
  "scripts": {
    "test": "node check.mjs"
  },
  "devDependencies": {
    "playwright": "^1.56.0"
  }
}
